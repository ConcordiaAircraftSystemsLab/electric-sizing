from setuptools import setup, find_packages

setup(
    name="electric_sizing",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # List your package dependencies here
    ],
    entry_points={
        # If you have any entry points (e.g., console scripts), specify them here
    },
)
